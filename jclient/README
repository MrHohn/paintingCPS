
Make sure you have installed the MFAPI on the machine. To complile the codes, enter:

	javac *.java -cp jmfapi-1.0-SNAPSHOT.jar

Usage:
JCLient <dst_GUID> <src_GUID>

compile: 
	javac *.java -cp jmfapi-1.0-SNAPSHOT.jar
	# or simply use makefile to compile it
	make

run: 
	# set myGUID as 101, otherGUID as 102
	sudo java -cp .:jmfapi-1.0-SNAPSHOT.jar TestClient 101 102
	# or simply use makefile to run it
	make run

PS:
This java client run in orbit mode in default. Please start the mf stack before run this program.
